server:
  port: 8080

spring:
  application:
    name: api-gateway

logging:
  level:
    com.smartlogix.gateway: INFO

jwt:
  secret: ${JWT_SECRET:SmartLogixSecretKeyUltraSegura2026ParaDesarrolloLocal}
  expiration: ${JWT_EXPIRATION:3600000}
